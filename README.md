# airflow-etl-event-pipeline
Built an Apache Airflow pipeline that ingests synthetic event records, validates and categorizes messages, loads structured customer/product/review data into PostgreSQL, and removes processed records after successful execution.
